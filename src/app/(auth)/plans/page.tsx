import { PlansPage } from "@/components/auth";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Choose Your Plan"
}

const PlansRoute = () => <PlansPage />

export default PlansRoute;
